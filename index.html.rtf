<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Design Portfolio</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Space+Mono:wght@400;700&display=swap');
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        :root {
            --primary-red: #A0463C;
            --primary-blue: #4A6B7C;
            --primary-yellow: #C2A878;
            --neutral-dark: #2D2D2D;
            --neutral-medium: #6B6B6B;
            --neutral-light: #E8E5E0;
            --background: #F5F3F0;
        }
        
        body {
            font-family: 'Space Mono', monospace;
            background-color: var(--background);
            color: var(--neutral-dark);
            line-height: 1.4;
            overflow-x: hidden;
        }
        
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }
        
        /* Header */
        header {
            position: relative;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background: linear-gradient(135deg, var(--background) 0%, var(--neutral-light) 100%);
        }
        
        .hero {
            text-align: center;
            z-index: 10;
            position: relative;
        }
        
        .hero h1 {
            font-size: clamp(3rem, 8vw, 8rem);
            font-weight: 700;
            letter-spacing: -0.05em;
            margin-bottom: 1rem;
            text-transform: uppercase;
            color: var(--neutral-dark);
        }
        
        .hero p {
            font-size: 1.2rem;
            color: var(--neutral-medium);
            margin-bottom: 2rem;
            letter-spacing: 0.1em;
        }
        
        /* Geometric shapes */
        .shape {
            position: absolute;
            pointer-events: none;
        }
        
        .shape-1 {
            width: 200px;
            height: 200px;
            background: var(--primary-red);
            top: 15%;
            left: 10%;
            clip-path: polygon(0 0, 100% 0, 50% 100%);
            animation: float 6s ease-in-out infinite;
        }
        
        .shape-2 {
            width: 150px;
            height: 150px;
            background: var(--primary-blue);
            top: 60%;
            right: 15%;
            border-radius: 50%;
            animation: float 4s ease-in-out infinite reverse;
        }
        
        .shape-3 {
            width: 100px;
            height: 300px;
            background: var(--primary-yellow);
            top: 20%;
            right: 30%;
            animation: float 5s ease-in-out infinite;
        }
        
        .shape-4 {
            width: 120px;
            height: 120px;
            background: var(--neutral-medium);
            bottom: 20%;
            left: 20%;
            transform: rotate(45deg);
            animation: float 7s ease-in-out infinite;
        }
        
        @keyframes float {
            0%, 100% { transform: translateY(0px) rotate(0deg); }
            50% { transform: translateY(-20px) rotate(5deg); }
        }
        
        /* Navigation */
        nav {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            padding: 2rem 0;
            background: rgba(245, 243, 240, 0.95);
            backdrop-filter: blur(10px);
            z-index: 100;
            border-bottom: 1px solid var(--neutral-light);
        }
        
        nav ul {
            list-style: none;
            display: flex;
            justify-content: center;
            gap: 3rem;
        }
        
        nav a {
            text-decoration: none;
            color: var(--neutral-dark);
            font-weight: 700;
            font-size: 0.9rem;
            text-transform: uppercase;
            letter-spacing: 0.1em;
            transition: all 0.3s ease;
            position: relative;
        }
        
        nav a::after {
            content: '';
            position: absolute;
            bottom: -5px;
            left: 0;
            width: 0;
            height: 2px;
            background: var(--primary-red);
            transition: width 0.3s ease;
        }
        
        nav a:hover::after {
            width: 100%;
        }
        
        /* Sections */
        section {
            padding: 6rem 0;
            position: relative;
        }
        
        .section-title {
            font-size: 3rem;
            font-weight: 700;
            margin-bottom: 3rem;
            text-align: center;
            text-transform: uppercase;
            letter-spacing: 0.05em;
            color: var(--neutral-dark);
        }
        
        /* Portfolio Grid */
        .portfolio-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2rem;
            margin-top: 3rem;
        }
        
        .portfolio-item {
            background: white;
            border: 3px solid var(--neutral-dark);
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }
        
        .portfolio-item:hover {
            transform: translateY(-5px);
            box-shadow: 10px 10px 0 var(--primary-red);
        }
        
        .portfolio-item::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 8px;
            background: linear-gradient(90deg, var(--primary-red) 33%, var(--primary-blue) 66%, var(--primary-yellow) 100%);
        }
        
        .portfolio-content {
            padding: 2rem;
        }
        
        .portfolio-content h3 {
            font-size: 1.5rem;
            margin-bottom: 1rem;
            text-transform: uppercase;
            letter-spacing: 0.05em;
        }
        
        .portfolio-content p {
            color: var(--neutral-medium);
            margin-bottom: 1.5rem;
        }
        
        .portfolio-placeholder {
            height: 200px;
            background: linear-gradient(45deg, var(--neutral-light) 25%, transparent 25%, transparent 75%, var(--neutral-light) 75%, var(--neutral-light)),
                        linear-gradient(45deg, var(--neutral-light) 25%, transparent 25%, transparent 75%, var(--neutral-light) 75%, var(--neutral-light));
            background-size: 20px 20px;
            background-position: 0 0, 10px 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.2rem;
            color: var(--neutral-medium);
            text-transform: uppercase;
            letter-spacing: 0.1em;
        }
        
        /* About Section */
        .about-content {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 4rem;
            align-items: center;
        }
        
        .about-text {
            font-size: 1.1rem;
            line-height: 1.8;
            color: var(--neutral-medium);
        }
        
        .about-visual {
            height: 400px;
            background: var(--primary-blue);
            position: relative;
            overflow: hidden;
        }
        
        .about-visual::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            width: 60%;
            height: 60%;
            background: var(--primary-yellow);
            transform: translate(-50%, -50%) rotate(45deg);
        }
        
        .about-visual::after {
            content: '';
            position: absolute;
            bottom: 20px;
            right: 20px;
            width: 80px;
            height: 80px;
            background: var(--primary-red);
            border-radius: 50%;
        }
        
        /* Contact Section */
        .contact {
            background: var(--neutral-dark);
            color: var(--neutral-light);
        }
        
        .contact-content {
            text-align: center;
        }
        
        .contact-info {
            display: flex;
            justify-content: center;
            gap: 4rem;
            margin-top: 3rem;
        }
        
        .contact-item {
            text-align: center;
        }
        
        .contact-item h3 {
            font-size: 1.2rem;
            margin-bottom: 1rem;
            text-transform: uppercase;
            letter-spacing: 0.1em;
        }
        
        .contact-item p {
            color: var(--neutral-medium);
        }
        
        .contact-item a {
            color: var(--primary-yellow);
            text-decoration: none;
            transition: color 0.3s ease;
        }
        
        .contact-item a:hover {
            color: var(--primary-red);
        }
        
        /* Footer */
        footer {
            background: var(--neutral-dark);
            color: var(--neutral-medium);
            text-align: center;
            padding: 2rem 0;
            border-top: 1px solid var(--neutral-medium);
        }
        
        /* Responsive */
        @media (max-width: 768px) {
            .about-content {
                grid-template-columns: 1fr;
            }
            
            .contact-info {
                flex-direction: column;
                gap: 2rem;
            }
            
            nav ul {
                gap: 1.5rem;
            }
            
            .shape-1, .shape-2, .shape-3, .shape-4 {
                opacity: 0.5;
            }
        }
    </style>
</head>
<body>
    <nav>
        <ul>
            <li><a href="#home">Home</a></li>
            <li><a href="#work">Work</a></li>
            <li><a href="#about">About</a></li>
            <li><a href="#contact">Contact</a></li>
        </ul>
    </nav>

    <header id="home">
        <div class="shape shape-1"></div>
        <div class="shape shape-2"></div>
        <div class="shape shape-3"></div>
        <div class="shape shape-4"></div>
        
        <div class="hero">
            <h1>Design</h1>
            <p>Portfolio • Studio • Creative</p>
        </div>
    </header>

    <section id="work">
        <div class="container">
            <h2 class="section-title">Selected Works</h2>
            <div class="portfolio-grid">
                <div class="portfolio-item">
                    <div class="portfolio-placeholder">Project Image</div>
                    <div class="portfolio-content">
                        <h3>Brand Identity</h3>
                        <p>Complete visual identity system for emerging tech startup</p>
                    </div>
                </div>
                <div class="portfolio-item">
                    <div class="portfolio-placeholder">Project Image</div>
                    <div class="portfolio-content">
                        <h3>Editorial Design</h3>
                        <p>Magazine layout and typography exploration</p>
                    </div>
                </div>
                <div class="portfolio-item">
                    <div class="portfolio-placeholder">Project Image</div>
                    <div class="portfolio-content">
                        <h3>Digital Experience</h3>
                        <p>Interactive web platform with geometric approach</p>
                    </div>
                </div>
                <div class="portfolio-item">
                    <div class="portfolio-placeholder">Project Image</div>
                    <div class="portfolio-content">
                        <h3>Packaging Design</h3>
                        <p>Minimalist approach to consumer product packaging</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <section id="about">
        <div class="container">
            <h2 class="section-title">About</h2>
            <div class="about-content">
                <div class="about-text">
                    <p>Influenced by the Bauhaus movement's principles of form following function, I create designs that balance aesthetic beauty with practical purpose. My work explores the intersection of geometric abstraction and human-centered design.</p>
                    <br>
                    <p>With a focus on clean lines, thoughtful typography, and carefully considered color palettes, I aim to create visual solutions that communicate clearly while maintaining artistic integrity.</p>
                </div>
                <div class="about-visual"></div>
            </div>
        </div>
    </section>

    <section id="contact" class="contact">
        <div class="container">
            <div class="contact-content">
                <h2 class="section-title">Get In Touch</h2>
                <div class="contact-info">
                    <div class="contact-item">
                        <h3>Email</h3>
                        <p><a href="mailto:hello@designer.com">hello@designer.com</a></p>
                    </div>
                    <div class="contact-item">
                        <h3>Phone</h3>
                        <p>+1 (555) 123-4567</p>
                    </div>
                    <div class="contact-item">
                        <h3>Location</h3>
                        <p>New York, NY</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <footer>
        <div class="container">
            <p>&copy; 2025 Design Portfolio. All rights reserved.</p>
        </div>
    </footer>

    <script>
        // Smooth scrolling for navigation links
        document.querySelectorAll('nav a').forEach(link => {
            link.addEventListener('click', function(e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                target.scrollIntoView({
                    behavior: 'smooth',
                    block: 'start'
                });
            });
        });

        // Parallax effect for shapes
        window.addEventListener('scroll', () => {
            const scrolled = window.pageYOffset;
            const shapes = document.querySelectorAll('.shape');
            
            shapes.forEach((shape, index) => {
                const speed = 0.5 + (index * 0.1);
                shape.style.transform = `translateY(${scrolled * speed}px)`;
            });
        });

        // Portfolio item hover effects
        document.querySelectorAll('.portfolio-item').forEach(item => {
            item.addEventListener('mouseenter', function() {
                this.style.transform = 'translateY(-5px) scale(1.02)';
            });
            
            item.addEventListener('mouseleave', function() {
                this.style.transform = 'translateY(0) scale(1)';
            });
        });
    </script>
</body>
</html>